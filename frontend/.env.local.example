# API 베이스 URL (사내 백엔드 / 로컬 / staging / production 별 분리)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# MSW Mock 활성화 여부 (개발 단계에서 백엔드 부재 시 true)
NEXT_PUBLIC_API_MOCKING=enabled

# 기본 사용자 권한 (개발용)
NEXT_PUBLIC_DEFAULT_USER_ROLE=sales
